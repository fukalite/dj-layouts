# dj-layouts

A Django layout composition library.

Views return their own content as partials. A `Layout` class assembles the full page by calling other views as named panels — concurrently under ASGI.

---

*Documentation coming soon.*
